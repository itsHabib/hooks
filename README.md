# workbench-hooks

LLM-side git hooks for the personal workbench — deterministic harness-level scripts that race the agent to mechanical metadata-ferrying between dossier / ship / gh.

This is the placeholder initial commit. The actual scaffold (README detail, Makefile, `scripts/`, `lib/`, `examples/`, tests) lands in the first PR — see `integration-layer/scaffold-and-stop-hook`.
