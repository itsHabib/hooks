#!/usr/bin/env bash
# Shared helper stub — populated by gh-pr-* hooks (wave-2).
# Find the dossier task linked to the current pull request.

set -euo pipefail

# shellcheck disable=SC2317
_pr_lookup_stub() {
  :
}
