#!/usr/bin/env bash
# Shared helper stub - populated by integration hooks (wave-2).
# Thin wrapper around the dossier binary with common args.

set -euo pipefail

# shellcheck disable=SC2317
_dossier_cli_stub() {
  :
}
