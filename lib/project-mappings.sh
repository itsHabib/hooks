#!/usr/bin/env bash
# Shared helper stub — populated by session-start-digest (wave-2).
# Map working directory paths to dossier project slugs.

set -euo pipefail

# shellcheck disable=SC2317
_project_mappings_stub() {
  :
}
