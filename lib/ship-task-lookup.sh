#!/usr/bin/env bash
# Shared helper stub — populated by ship-ship-done-hook (wave-2).
# Resolve spec-doc paths to dossier task identifiers.

set -euo pipefail

# shellcheck disable=SC2317
_ship_task_lookup_stub() {
  :
}
